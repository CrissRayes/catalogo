<%= form_with(model: medio_pago) do |form| %>
  <% if medio_pago.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(medio_pago.errors.count, "error") %> prohibited this medio_pago from being saved:</h2>

      <ul>
        <% medio_pago.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
